syntax = "proto3";

package resolve;

service resolve {
    rpc resolveStream (Source) returns (stream address);
    rpc resolve (Source) returns ( address);

}
message Source {
    string name = 1;

}
message address {
    string name = 1;
}

message allAddresses {
    repeated address address =1 ;
}